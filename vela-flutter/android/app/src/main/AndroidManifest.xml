<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The engine and the system images are downloaded at runtime. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Legacy external-storage perms: the optional ~/.vela on shared storage
         and the "save engine logs" action. Ignored from API 33 onwards. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- 引擎与工具链靠 APK 自带的可执行负载运行：内核只允许执行本应用 /data/app/**
         /lib/<abi>/ 下的文件（标签 apk_data_file），私有目录里的会被 SELinux 拒绝。
         所以 manifest 里不再需要 debuggable，也不再需要 run-as / Shizuku。 -->
    <application
        android:label="Vortex"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.velasim.app.VelaFlutterActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 无界面入口（脚本/AI 用）：adb shell 可调，普通应用不行。 -->
        <provider
            android:name="com.velasim.app.VelaHeadlessProvider"
            android:authorities="com.velasim.app.headless"
            android:exported="true"
            android:permission="android.permission.DUMP" />
        <!-- 工程页「用其他应用打开」：把构建出来的 rpk 以 content:// 交给别的应用
             （Android 7 起 file:// 会抛 FileUriExposedException）。不 exported，
             只按次授权读，路径限制在 files/ 与外部存储根内。 -->
        <provider
            android:name="com.velasim.app.VelaFileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true" />
        <service
            android:name="com.velasim.app.VelaHeadlessService"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="runs the bundled on-device emulator engine for scripted headless use" />
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Required by Flutter's ProcessTextPlugin; see
             https://developer.android.com/guide/topics/manifest/queries-element.html -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 「用其他应用打开」要先问系统有哪些应用能接 rpk（VIEW / 分享），
             Android 11+ 不问就查不到，选择器会空着。 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>
</manifest>
